

### 最终需求文档 (V2)：为 Next.js 项目实现 Hugo 式 Markdown 内容管理

**1. 🎯 核心目标 (Overall Goal)**

在我现有的 Next.js 项目中，实现一个基于本地 Markdown 文件的内容管理系统。我希望通过在 `content` 文件夹中添加/修改 `.mdx` 文件，来自动创建、更新我的**博客文章**和**项目展示**页面。

**2. 🔑 关键需求 (Key Requirements)**

  * **双内容类型：** 系统必须能区分“**博客 (Post)**”和“**项目 (Project)**”。
  * **Markdown 驱动：** 所有内容源自 `.mdx` 文件。
  * **Front Matter 解析：** 必须能解析每个 `.mdx` 文件头部的 YAML Front Matter。
  * **静态生成 (SSG)：** 所有内容详情页（如 `/blog/my-post`）应在构建时生成为静态 HTML。
  * **本地工作流：** 最终体验是：本地创建 `.mdx` -\> `git push` -\> 网站自动部署更新。

**3. 🗂️ 内容模型与目录结构 (Data Models & Directory Structure)**

**目录结构:**

```
content/
├── blog/     <-- (存放 Post 类型)
└── projects/ <-- (存放 Project 类型)
```

**A. 内容模型：`Post` (博客文章)**

  * **路径：** `content/blog/**/*.mdx`
  * **Front Matter 字段：**
      * `title` (string): 文章标题
      * `date` (date): 发布日期
      * `image` (string): 封面图片 URL
      * `description` (string): 简短描述
      * `categories` (list[string]): **分类（唯一的分类字段）**
  * **计算字段：**
      * `readingTime` (string): 阅读时长（需要自动计算）。

**B. 内容模型：`Project` (项目展示)**

  * **路径：** `content/projects/**/*.mdx`
  * **Front Matter 字段：**
      * `title` (string): 项目标题
      * `status` (string): 项目状态 (e.g., "进行中", "已完成")
      * `image` (string): 封面图片 URL
      * `description` (string): 项目简介
      * `tags` (list[string]): **技术栈（用于展示和过滤）**
      * `github_url` (string, optional): GitHub 链接

**4. 📄 页面实现 (Page Implementation)**

1.  **项目列表页 (`/projects`)**

      * **数据加载：** 页面在构建时获取**所有** `Project` 内容。
      * **过滤功能 (客户端)：**
          * 页面顶部有一个**技术栈标签过滤栏** (基于 `Project` 的 `tags` 字段动态生成)。
          * 点击某个标签（如 "Python"），页面**不会跳转**，而是**在客户端**（使用 React State）实时过滤下方显示的项目卡片列表。
          * 需要一个 "全部" 按钮来清除过滤。
      * **卡片渲染：** 按要求显示 `title`, `status`, `description`, `tags` 等。

2.  **博客列表页 (`/blog`)**

      * **数据加载：** 页面在构建时获取**所有** `Post` 内容，并按 `date` 倒序排序。
      * **过滤功能 (客户端)：**
          * 页面顶部有一个**分类过滤栏** (基于 `Post` 的 `categories` 字段动态生成)。
          * 点击某个分类（如 "技术教程"），页面**不会跳转**，而是**在客户端**实时过滤下方显示的博客卡片列表。
          * 需要一个 "全部" 按钮来清除过滤。
      * **卡片渲染：** 按要求显示 `image`, `categories`, `title`, `description`, `date`, `readingTime`。

3.  **详情页 (`/blog/[slug]` 和 `/projects/[slug]`)**

      * 为每一个 `.mdx` 文件生成一个对应的静态详情页。
      * 页面需要能正确渲染 `.mdx` 文件的正文内容。

**5. 💡 推荐技术栈**

  * **Contentlayer** (或 **Velite**)：用于实现类型安全的内容解析和管理。

