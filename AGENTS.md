# Repository Guidelines

## Git 提交规范

本仓库使用 Conventional Commits。提交记录必须让维护者仅通过提交信息就能理解修改目的、具体影响和验证方式。

### 提交标题

统一使用中文，格式如下：

```text
<type>(<scope>): <明确描述用户可感知的变化>
```

标题应描述实际结果，不得使用 `update`、`refine`、`modify files`、`调整代码`、`优化一下` 等含糊表达；末尾不加句号。

### Type

- `feat`：新增功能或用户可感知的行为变化。
- `fix`：修复错误、异常行为或展示问题。
- `refactor`：不改变外部行为的结构调整。
- `content`：新增或修改博客、项目等站点内容。
- `style`：纯视觉样式变化，不包含功能行为变化。
- `perf`：性能优化。
- `test`：测试新增或调整。
- `docs`：开发文档、说明和仓库规范。
- `build`：依赖、构建工具和构建配置。
- `chore`：生成文件及其他维护性工作。
- `revert`：撤销已有提交。

### Scope

优先使用以下 scope：

- `blog`：博客列表及博客整体功能。
- `blog-card`：博客卡片。
- `blog-detail`：博客详情页。
- `content`：文章、分类和内容体系。
- `project`：项目展示。
- `search`：站内搜索。
- `theme`：主题、颜色和字体。
- `navigation`：导航栏。
- `build`：构建系统。
- `repo`：仓库级规范和配置。

一次修改涉及同一模块的多个子区域时，使用较高层级的 scope，避免 scope 过度细碎。

### 提交正文

涉及多个文件、多个行为或需要解释取舍时，必须添加正文：

```text
feat(blog): 精简博客列表与详情页内容展示

- 移除博客卡片摘要，避免长标题与摘要拥挤
- 固定标题区域高度并缩短卡片内容区
- 移除详情页自动封面，保留列表封面和正文图片
- 重新生成站内搜索索引

验证:
- npm run build
```

正文说明实际完成的修改、重要保留行为及已执行的验证，不记录未执行的测试。

### 提交原则

- 一个提交只处理一个明确主题；不同目的的修改拆分提交。
- 提交前检查 `git status`、工作区 diff 和暂存区 diff。
- 明确指定需要暂存的文件，不使用 `git add .` 混入无关修改。
- 不修改、覆盖或提交用户正在进行的无关变更。
- 构建生成文件应与导致其变化的源码一起提交。
- 提交前执行与风险相匹配的测试或构建，并在正文记录结果。
- 提交后检查工作区状态和最新提交信息。
- 破坏性变更使用 `<type>(<scope>)!:`，并在正文中添加 `BREAKING CHANGE:`。
- 未经用户明确授权，不改写已推送的提交历史。
